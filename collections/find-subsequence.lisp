(defun count-subsequence (mainstr substr)
  (let ((pos 0)
        (counter 0)
        (main-len (length mainstr))
        (sub-len (length substr)))
    (loop while (< (+ pos sub-len) main-len) do
         (when ())))
