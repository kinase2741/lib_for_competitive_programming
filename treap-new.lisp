(let ((root nil))

  (defun node-generator ()
    (let ((cnt 0)
          (left nil)
          (right nil))
      ;; add
      ;; sub
      ;; get-element
      ;; get-min
      ;; get-max
      (values (lambda ())))))

